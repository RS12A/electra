"""
Django admin configuration for the admin API module.

This module provides Django admin interface integration for admin API
models and utilities. Currently serves as a placeholder as the admin
API module primarily uses DRF APIs rather than Django admin interface.
"""
from django.contrib import admin

# No models to register in this module as it provides API interfaces
# Admin functionality is handled through the REST API endpoints